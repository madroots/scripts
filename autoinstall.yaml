#cloud-config
autoinstall:
  version: 1
  identity:
    hostname: ubuntu-server
    username: master
    password: "$6$rounds=4096$9DEfpflFntY4Embl$8XoX9Z2bK76iV7Y.8G.mW5PPrZf2zTdpwMcuUMpe78uP7yCfPAzMyU7LqJfyHzJaxlNoFQEVwcqCWnM2mN8Ku1"  # admin
  ssh:
    install-server: true
    allow-pw: true
  packages:
    - curl
    - git
  locale: en_US.UTF-8
  keyboard:
    layout: us
  timezone: Europe/Bratislava
  storage:
    layout:
      name: lvm
  updates: security
  user-data:
    disable_root: false
  late-commands:
    - curtin in-target --target=/target bash -c "echo 'PermitRootLogin yes' >> /etc/ssh/sshd_config"
    - curtin in-target --target=/target systemctl enable ssh
